# Caddy 反向代理配置（HTTP 模式）
# 部署初期证书未就绪：http:// 前缀强制纯 HTTP（不申请证书、不监听 443）
# 证书就绪后：删除 http:// 前缀并重启容器，Caddy 自动签发证书并启用 HTTPS

http://shaoyuanyu.cn {
    reverse_proxy web:3000
    encode gzip
}

http://umami.shaoyuanyu.cn {
    reverse_proxy umami:3000
    encode gzip
}
